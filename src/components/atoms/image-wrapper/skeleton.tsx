import { styled } from 'styled-components';

import { StyledDiv as ImageWrapper } from './image-wrapper';

export const StyledDiv = styled(ImageWrapper)`
  aspect-ratio: 16 / 9;
  min-height: 300px;
`;
