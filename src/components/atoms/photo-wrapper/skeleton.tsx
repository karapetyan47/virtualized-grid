import { styled } from 'styled-components';

import { StyledDiv as PhotoWrapper } from './photo-wrapper';

export const StyledDiv = styled(PhotoWrapper)`
  aspect-ratio: 16 / 9;
  min-height: 300px;
`;
